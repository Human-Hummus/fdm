include default
import std/times
stdout.write "{date=" & getTime().format("MMMM d, YYYY") & "}"
stdout.write "{times=×}"
stdout.write "{plus_minus=±}"
stdout.write "{divide=÷}"
stdout.write "{interrobang=‽}"
